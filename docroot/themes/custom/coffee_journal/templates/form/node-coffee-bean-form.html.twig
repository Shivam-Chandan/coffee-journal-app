{#
  Coffee Journal — Coffee Bean node add/edit form override
  node-coffee-bean-form.html.twig

  The cjFormScraper JS behaviour (app.js) handles URL auto-fill.
  The recipes section and Add Recipe button are injected via form_alter
  and will render within the {{ form }} call.
#}

{{ form }}
