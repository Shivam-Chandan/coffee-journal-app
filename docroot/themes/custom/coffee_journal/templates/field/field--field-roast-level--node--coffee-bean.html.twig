{#
  Coffee Journal — Roast Level badge template.
  roast_label and roast_key are set by coffee_journal_preprocess_field().
#}
{%- if roast_label -%}
  <span class="roast-badge roast-badge--{{ roast_key }}">{{ roast_label }}</span>
{%- endif -%}
